// import { AppBar, Toolbar, MenuItem } from "@material-ui/core/";
// import { useHistory } from "react-router-dom";

// const Menu = () => {
//   const history = useHistory();

//   const send = (page) => {
//     history.push(page);
//   };

//   return (
//     <AppBar>
//       <Toolbar>
//         <MenuItem onClick={() => send("/rickymorty")}>Ricky</MenuItem>
//         <MenuItem onClick={() => send("/pokemon")}>Pokemon</MenuItem>
//         <MenuItem onClick={() => send("/favorites")}>Favoritos</MenuItem>
//       </Toolbar>
//     </AppBar>
//   );
// };

// export default Menu;
